mixin post(post)
  div
    h4
      a(href=`/post/${post.id}`)= `[${post.prefix}] ${post.title}`
    p=`👍: ${post.meta.like}`
    small= post.toFormattedDate()  
    hr

            
            


